import { ReqStatusPipePipe } from './req-status.pipe-pipe';

describe('ReqStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReqStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
